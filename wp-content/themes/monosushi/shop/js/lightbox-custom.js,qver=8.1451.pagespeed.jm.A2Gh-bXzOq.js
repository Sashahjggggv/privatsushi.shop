jQuery(function($){'use strict';let lightBoxOptions={disableScroll:false,captionSelector:'self',closeText:'<div class="btn-close"></div>',navText:['<div class="swiper-button-prev"><span class="arr"></span></div>','<div class="swiper-button-next"><span class="arr"></span></div>'],alertErrorMessage:"No image found. Next will be load.",history:false,docClose:false,heightRatio:0.8},lightboxSelector=$('.lightbox-wrapper'),lightboxLength=lightboxSelector.length,lightbox=[];for(let i=0;i<lightboxLength;i++){lightbox[i]=$(lightboxSelector[i]).find('.lightbox').simpleLightbox(lightBoxOptions);$(lightboxSelector[i]).find('.lightbox').on('shown.simplelightbox',_functions.removeScroll);$(lightboxSelector[i]).find('.lightbox').on('close.simplelightbox',_functions.addScroll);};$(document).on('click','.sl-overlay',function(){lightbox[0].close();});});